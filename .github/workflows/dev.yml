name: Dev Branch Workflow

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

jobs:
  dev_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Run Dev Tasks
        run: echo "Running tasks for dev branch"
