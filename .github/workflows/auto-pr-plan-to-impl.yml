name: Auto PR from plan to impl
on:
  workflow_run:
    workflows: ["Plan Branch Workflow"]
    branches:
      - plan
    types:
      - completed
permissions:
  pull-requests: write
  contents: read
jobs:
  create-pull-request:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Create Pull Request
        uses: actions/github-script@v7
        with:
          script: |
            const head = 'plan';
            const base = 'impl';
            const { data: pulls } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: `${context.repo.owner}:${head}`,
              base: base,
              state: 'open'
            });
            if (pulls.length === 0) {
              await github.rest.pulls.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                head: head,
                base: base,
                title: 'Sync impl with plan',
                body: 'Automated PR to sync impl branch with plan.'
              });
            } else {
              console.log('Pull request already exists');
            }
