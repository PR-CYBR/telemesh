name: Stage Branch Workflow

on:
  push:
    branches:
      - stage
  pull_request:
    branches:
      - stage

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Stage job
        run: echo "Running stage branch tasks"
