name: Auto PR from spec to plan
on:
  workflow_run:
    workflows: ["Spec Branch Workflow"]
    branches:
      - spec
    types:
      - completed
permissions:
  pull-requests: write
  contents: read
jobs:
  create-pull-request:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Create Pull Request
        uses: actions/github-script@v7
        with:
          script: |
            const head = 'spec';
            const base = 'plan';
            const { data: pulls } = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: `${context.repo.owner}:${head}`,
              base: base,
              state: 'open'
            });
            if (pulls.length === 0) {
              await github.rest.pulls.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                head: head,
                base: base,
                title: 'Sync plan with spec',
                body: 'Automated PR to sync plan branch with spec.'
              });
            } else {
              console.log('Pull request already exists');
            }
